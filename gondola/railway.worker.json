{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "gondola/Dockerfile"
  },
  "deploy": {
    "startCommand": "python -m gondola.app.worker",
    "restartPolicyType": "ALWAYS",
    "numReplicas": 1
  }
}
